apiVersion: serving.knative.dev/v1 # Current version of Knative
kind: Service
metadata:
  name: payroll-h2 # The name of the app
  namespace: default # The namespace the app will use
spec:
  template:
    spec:
      containers:
        - image: https://hub.docker.com/repository/docker/banupkubeforce/springcloudfunctions:main  # generated by kpack